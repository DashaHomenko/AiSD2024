struct ListNode {
    int item;          // Данные
    ListNode* next;    // Указатель на следующий узел

    // Конструктор для удобства инициализации
    ListNode(int val) : item(val), next(nullptr) {}
};


/* В этом коде:

1. Мы используем struct для определения узла связного списка.
2. Поле item хранит данные узла.
3. Поле next является указателем на следующий узел в списке.
4. Добавлен конструктор для удобства инициализации узла.

Этот код на C++ эквивалентен приведённому вами коду на C, но использует возможности C++ для более удобной работы с структурами. */
