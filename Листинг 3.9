void traverse_tree(TreeNode* t) {
    if (t != nullptr) {
        traverse_tree(t->left);   // Рекурсивный обход левого поддерева
        process_item(t->item);    // Обработка текущего узла
        traverse_tree(t->right); // Рекурсивный обход правого поддерева
    }
}


/* В этом коде:

1. Мы используем nullptr вместо NULL, что является стандартным в C++.
2. Функция traverse_tree выполняет симметричный обход (in-order traversal) двоичного дерева.
3. Функция process_item должна быть реализована для обработки значения узла. Если у вас её нет, её нужно будет добавить. */
