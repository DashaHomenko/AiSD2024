struct TreeNode {
    int item;           // Элемент данных
    TreeNode* parent;   // Указатель на родительский узел
    TreeNode* left;     // Указатель на левый дочерний узел
    TreeNode* right;    // Указатель на правый дочерний узел

    // Конструктор для удобства инициализации
    TreeNode(int val, TreeNode* p = nullptr, TreeNode* l = nullptr, TreeNode* r = nullptr)
        : item(val), parent(p), left(l), right(r) {}
};


/*В этом коде:

1. Мы используем struct для определения узла дерева.
2. Поля item, parent, left и right соответствуют элементам данных и указателям на родительский, левый и правый дочерние узлы.
3. Добавлен конструктор для удобства инициализации узла. Конструктор позволяет задать значение узла и указатели на родительский и дочерние узлы при создании объекта.

 используем возможности C++ для более удобной работы с структурами и инициализацией. */
